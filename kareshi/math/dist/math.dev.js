"use strict";

// variables
var tsu = 5;
var ma = 3;
var tsuma; // basic math

tsuma = tsu + ma;
tsuma = tsu - ma;
tsuma = tsu * ma;
tsuma = tsu / ma;
tsuma = tsu % ma; // Math Object

tsuma = Math.PI;
tsuma = Math.E;
tsuma = Math.round(tsu / ma);
tsuma = Math.ceil(tsu / ma);
tsuma = Math.floor(tsu / ma);
tsuma = Math.sqrt(tsu / ma);
tsuma = Math.abs(-tsu / ma);
tsuma = Math.pow(tsu, ma);
tsuma = Math.min(-tsu, ma);
tsuma = Math.max(tsu, -ma); // random numbers

tsuma = Math.random(); // random decimal number between 0 and 1

tsuma = Math.random() * 10; // random number between 0 and 10 - till 9

tsuma = Math.random() * 10 + 1; // random number between 1 and 10 - including 10

tsuma = Math.floor(tsuma); //console output

console.log(tsuma);